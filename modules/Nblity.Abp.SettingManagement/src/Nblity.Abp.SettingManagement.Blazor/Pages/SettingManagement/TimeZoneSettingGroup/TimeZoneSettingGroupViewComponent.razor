@using Volo.Abp.SettingManagement.Localization
@inherits Volo.Abp.AspNetCore.Components.AbpComponentBase
@inject AbpBlazorMessageLocalizerHelper<AbpSettingManagementResource> LH

@if (TimezoneSettings != null)
{
    <MudGrid>
        <MudItem xs="12">
            <MudSelect T="string" Label="@($"{L["DisplayName:Timezone"]} *")" Value="TimezoneSettings.Timezone" ValueChanged="OnSelectedValueChangedAsync">
                @foreach (var item in TimezoneSettings.TimeZoneItems)
                {
                    <MudSelectItem Value="item.Value">@item.Name</MudSelectItem>
                }
            </MudSelect>
            <MudText Typo="Typo.caption" Class="mt-1">@L["TimezoneHelpText"].Value</MudText>
        </MudItem>
        <MudItem xs="12">
            <MudButton Color="MudBlazor.Color.Primary" Variant="Variant.Filled" OnClick="@UpdateSettingsAsync">@L["Save"]</MudButton>
        </MudItem>
    </MudGrid>
}
